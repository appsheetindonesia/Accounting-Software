{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "Mock API server Appsheet Accounting Journal — implementasi API - Accounting.md dengan logika akuntansi nyata (double-entry)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "reset": "node scripts/reset.js",
    "seed:extra": "node scripts/reset.js --extra",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mock",
    "api",
    "accounting",
    "express"
  ],
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "supertest": "^7.1.1",
    "vitest": "^3.2.0"
  }
}
