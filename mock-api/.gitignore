# State persistence mock API — data pengguna, jangan di-commit
.data/

# Log runtime mock API — mock-api.log, *.err, nohup.out, dsb.
# (aturan khusus sub-proyek ini agar tidak pernah ter-commit
#  walau aturan root *.log dihapus/diubah)
*.log
*.err
nohup.out

# Laporan test JUnit (dihasilkan CI saat gagal; tidak pernah di-commit)
test-results/

# Laporan Vitest coverage (dihasilkan CI; tidak pernah di-commit)
coverage/
