# Root adalah monorepo skrip (bukan package npm) — jangan buat package-lock.json
# di root. Setiap sub-proyek (mock-api, prototype-accounting, e2e) punya
# lockfile-nya sendiri yang tetap dihormati.
package-lock=false
