# Direktori internal tooling
.freebuff/
.agents/

# Dependensi & artefak build
node_modules/
dist/

# package-lock.json di ROOT — repo ini bukan package terpisah (sub-proyek punya
# lockfile masing-masing); file kosong yang ter-generate npm tidak boleh masuk.
/package-lock.json

# Artefak & file lokal
*.log
.DS_Store
Thumbs.db
